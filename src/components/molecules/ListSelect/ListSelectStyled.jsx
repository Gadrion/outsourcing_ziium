import styled from 'styled-components';
import { Select } from 'wisenet-ui/components/atoms';

const ListSelectStyled = styled(Select)`
  text-transform: capitalize;
`;

export default ListSelectStyled;
