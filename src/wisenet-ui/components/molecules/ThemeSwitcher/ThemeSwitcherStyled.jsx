import styled from 'styled-components';
import { Select } from 'wisenet-ui/components/atoms';

const ThemeSwitcherStyled = styled(Select)`
  text-transform: capitalize;
`;

export default ThemeSwitcherStyled;
